# Copy to .env and fill in. .env is gitignored — never commit a real key.
# Creator Hub -> Open Cloud -> API Keys. Needs universe-places with "write".
ROBLOX_API_KEY=
ROBLOX_UNIVERSE_ID=
ROBLOX_PLACE_ID=
